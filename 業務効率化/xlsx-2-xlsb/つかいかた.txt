■ xlsx → xlsb 変換ツール

Excelファイルを軽いバイナリ形式（.xlsb）に一括変換するツール。
「xlsx_to_xlsb_converter.html」をダブルクリックするとブラウザで開きます。
ネット接続は不要。ファイルは端末の外に送信されません。

【重要な注意】
  この変換で残るのは「値・数式・シート構成」だけです。
  セルの色・罫線・グラフ・図形・マクロ（VBA）・ピボットテーブルは消えます。
  必ず元の .xlsx を残したまま使ってください（元ファイルは変更されません）。
